import React from "react";
import logo from "./logo.svg";

const Navbar = () => {
  return (
    <header>
      <div id="header">
        <img id="logo" src={logo} />
      </div>
    </header>
  );
};

export default Navbar;
